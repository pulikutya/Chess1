#include"main.h"
#pragma once
#define abs(x) ((x) > 0? (x) : 0-(x))
#define sign(x) ((x) > 0? 1 : ((x) < 0? -1 : 0));
#define isPoitive(x) ((x) > 0)
#define isNegative(x) ((x) < 0)
#define neg(x) (0-(x))